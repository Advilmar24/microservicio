spring:
  application:
    name: auth

  datasource:
    url: jdbc:mysql://host.docker.internal:3306/auth_db
    username: root
    password: 1005367136md
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        "[format_sql]": true
        
server:
  port: 8084
  servlet:
    context-path: /api/v1/

logging:
  level:
    "[org.springframework]": INFO

security:
  jwt:
    # Clave secreta que se genera con openssl rand -base64 32
    secret-key: WCrD7ZwsNH2yZx4d29X3522BEAYTjm7BiNJxi9xksjU=
    # Expiracion expresada en ms (10 min)
    token-expiration: 6000000
