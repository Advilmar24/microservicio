spring:
  application:
    name: wishlist

  datasource:
    url: jdbc:mysql://host.docker.internal:3306/wishlist_db
    username: root
    password: 1005367136md
    driver-class-name: com.mysql.cj.jdbc.Driver

  
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        "[format_sql]": true

server:
  port: 8082
  servlet:
    context-path: /api/v1/

logging:
  level:
    "[org.springframework]": INFO